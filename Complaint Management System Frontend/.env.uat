# UAT build: same-origin /api. CRA does not load this file automatically.
# Build with: npx env-cmd -f .env.uat react-scripts build
# Confirm host with DNS if UAT is not same-origin.
REACT_APP_API_BASE_URL=
