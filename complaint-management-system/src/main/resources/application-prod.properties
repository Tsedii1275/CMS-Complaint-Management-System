# Production — same-origin HTTPS at F5/IIS/Nginx. Confirm host with DNS.
# Activate: SPRING_PROFILES_ACTIVE=prod
# Do not enable server.ssl here. Do not store the wildcard PFX on the JVM.
app.public-base-url=https://cms.dashenbanksc.com
app.cors.allowed-origins=https://cms.dashenbanksc.com
server.forward-headers-strategy=framework
